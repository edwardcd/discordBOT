const Discord = module.require("discord.js");

module.exports.run = async (bot, message, args) => {
    console.log("!roll works");
}

module.exports.help = {
    name: "roll",
    description: "basic dice roller XdY+Z"
}